<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="#666666"/>
            <corners android:radius="32dp"/>
        </shape>
    </item>

    <!-- Pressed -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/play_button_pressed"/>
            <corners android:radius="32dp"/>

            <stroke
                android:width="1dp"
                android:color="#D0D0D0"/>

            <padding
                android:left="16dp"
                android:top="16dp"
                android:right="16dp"
                android:bottom="16dp"/>
        </shape>
    </item>

    <!-- Focused -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="@color/play_button"/>
            <corners android:radius="32dp"/>

            <stroke
                android:width="2dp"
                android:color="@color/minebutton_color"/>
        </shape>
    </item>

    <!-- Default -->
    <item>
        <shape>
            <solid android:color="@color/play_button"/>
            <corners android:radius="32dp"/>

            <stroke
                android:width="1dp"
                android:color="#DDDDDD"/>

            <padding
                android:left="16dp"
                android:top="16dp"
                android:right="16dp"
                android:bottom="16dp"/>
        </shape>
    </item>

</selector>
